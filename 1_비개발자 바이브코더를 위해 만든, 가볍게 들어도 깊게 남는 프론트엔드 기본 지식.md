인터넷을 기반으로 돌아가는데 웹이나 다른 것들
웹은 그냥 문서공유 시스템이어따

1. 문서를 어떻게 쓰는지 규칙(HTML)
URL
HTTP(통신)
ㄴ 본문 내용 등 분리

문서마다 고유한 주소 부여

HTTP 문서를 요청하고 받는 통신규칙
대화의 형식(약속)

HTML파일을 그대로 보여주는 것이 예전

디자인 / 인터랙션 X 그냥 문서
예쁘게 만들고싶은 욕심

HTML에 코드로 심는 방법 -> 유지보수 지옥
HTML는 구조 전문인데 디자인 섞으니까 더 지옥

CSS등장

HTML + CSS등장

그래봤자 인터랙션이 없음
그게 바로 JAVA 스크립트

자바~랑 자바스크립트는 완전 다릅니다~!
  브라우저 - HTML + CSS + 자바스크립트
화면의 특정 부분만 바꿀수 있음
ㄴ 이벤트(사용자의 행동) + DOM 조작

브라우저가 내부 지도를 인식하고(DOM) 변경가능
공연 중에 무대를 바꾸는 행동과 같음(실시간으로 다른 연출)

반응하기 시작한 웹

~브라우저 전쟁~
  넷스케이프 / 익스플로러
마크업(약속)인데 CSS가 붙고 연출하는 것은 브라우저에서 이루어지는데
같은 HTML을 써도 다르게 보이는 상황이 있음
파이어폭스 크롬 에서도 작동하는지 -> 크로스 브라우저 이슈

브라우저의 버전따라도 견적 달라짐
브라우저가 어떻게 화면을 그리는지 순서

읽기(프레이징) -> 추리기(렌더트리) -> 레이아웃 -> 페인트(디자인)

리플로우(자바로 돔을 수정하면 레이아웃 수정을 다시 해야함)
프론트엔드 최적화의 하나 -> 리플로우를 얼마만큼 줄이냐

[근데 AI 바이브코딩이면 백이 더 중요한거아님 ? 프론트를 어디까지 알아야함 ? 백 포함]

라이브러리 시대
제이쿼리(크로스 브라우저 문제 어느정도 해결)
화면이 계속 복잡해지는, 코드를 계속 유지보수 해야함 -> 2013 페이스북의 리액트 출시

추상화
리액트의 컴포넌트(틀)
스테이트(재사용성)

프론트엔드는 화면꾸미기 x
퍼블리셔에서 프론트엔더로 넘어감

[왜 하는지 알려주네]

이 코드들을 어떻게 공유하고 협업할 수 있을까
여러 라이브러리가 충돌하거나 이런 상황들

2009년 NODE.JS 등장(런타임)
런타임 -> 실제 코드가 돌아가는 환경

NPM -> 패키지 매니저 #이건 좀 들어봄
2010년.프로그래밍 세계의 앱스토어

NPM INSTALL REACT

노드 패키지 매니저    
어느 컴퓨터에서는 동일한 환경 재현가능(디펜던시 관리)

프론트엔드 -> 조립식 개발

왜 이파일이 생겻지 ? -> 부품 목록을 알려주는 파일

개발 코드의 주석 / 변수 / 디버그용 코드

  ** 빌드(트랜스파일 / 번들링) **

  트랜스 파일린(바벨)
파일이 수십 - 수백개로 쪼개짐 -> 하나로 합쳐야함(번들링)
웹펙 / 바이트가 많이 쓰입니다

트리쉐이킹(NPM에서 쓰는 라이브러리 중에서 실제로 쓰이는 코드만 번들링)

프로덕션 최적화(파일 크기를 제일 최대한으로 줄이는 것)
여백.주석 메모 -> 제거
압축 -> 변수 이름을 짧게 바꿔서 크기를 줄이는 것

프로덕션의 웹 어플을 만든다 하면 패키지.JSON이 있는 상태
빌드 과정을 거쳐야 배포를 된다. (NPM RUN)

MPA(멀티 페이지 어플리케이션) -> 페이지마다 HTML이 존재


근데 폰은 부드럽게 전환이 됌.  "해줘"

SPA(싱글 페이지 어플리케이션) -> HTML이 하나만 존재
페이지마다 HTML이 존재하는 MPA는 페이지 이동마다 새로고침이 일어남
SPA는 페이지 이동이 일어나지 않음 -> 인터랙션이 더 부드러움

자바스크립트 / 클라이언트 사이드 라우팅
URL이 바뀌지만 페이지 이동이 일어나지 않음
페이지 이동이 일어나지 않으니까 인터랙션이 부드러움

화면을 교체만 한다.

웹이 앱처럼 행동하기 시작. -> 프론트엔드의 변화

SPA의 단점

백엔드 API에서 데이터를 받아서 화면에 뿌려주는 과정이 복잡해짐

어플리케이션 프로그래밍 인터페이스 -> 이건 알고잇지롱 메롱

프론트엔드 / 백엔드 분리 -> API로 통신

꽤나 큰 작업분리

ui 아키텍처 설게 < 상태관리 등등 프론트의 영역이 됨

SPA가 아쉬운 점 -> 초기 로딩이 느려짐
모든 화면을 가져와서 왓다갓다 보여줘야하기 때문

seO(검색엔진 최적화)
구글같은 검색 엔진이 인터렉션이 안보이니까, 빈껍데기만 보임
그래서 아무것도 없는 줄 알고 나감

ssr VS csr
SSR(서버 사이드 렌더링) -> 서버에서 HTML을 만들어서 보내주는 것
CSR(클라이언트 사이드 렌더링) -> 클라이언트에서 HTML을 만들어서 보여주는 것

하이드레이션(SSR로 받은 HTML을 CSR로 바꿔주는 과정)
NEXT.JS -> 리액트 프레임워크

OG태그(오픈 그래프 태그) -> 페이스북에서 만든 태그
 SSR환경에서가 아니면 동작이 힘듬

 트레이딩같은 개발환경
 SSR 쓰면 좋아요


#한줄씩 정리
 웹은 1989년 그냥 문서공유 시스템으로 시작
 HTML이 뼈대, CSS가 옷, HTTP가 대화의 형식

NPM 인스톨 

페이지 깜박임 없이 앱처럼~ -> SPA
SSR이 다시 등장

브라우저와 사용자 사이의 인터랙션이 점점 더 중요해짐 
그 영역에 ai 가 들어왔따. 
