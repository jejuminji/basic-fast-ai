# 🦣 우가우가 프론트엔드 용어집

> 원시인도 이해하는 화면 부품 사전. 우가!

화면 = 동굴 벽. 우리는 그 벽에 붙일 돌멩이 부품들을 다룬다. 우가우가!

---

## 1. 디자인 토큰 (Design Token)
가장 작은 약속. **"파랑은 이 파랑", "여백은 손가락 두 마디"** 처럼 색·크기·간격·글자굵기를 숫자로 못 박은 쪽지.
- 모든 부품이 이 쪽지 보고 똑같이 칠한다.
- 쪽지 한 장 바꾸면 → 동굴 전체 색 싹 바뀐다.
- 예: `color.primary`, `spacing.md`, `font.bold`

## 2. 컴포넌트 (Component)
부품 하나. 크기 단계가 있다 (= **아토믹 디자인**, Atomic Design):

| 단계 | 우가 비유 | 예시 |
|------|-----------|------|
| **아톰 (Atom)** | 더 못 쪼개는 알갱이 | 버튼, 입력칸, 글자 |
| **몰리큘 (Molecule)** | 알갱이 몇 개 뭉침 | 입력칸 + 검색버튼 = 검색바 |
| **오가니즘 (Organism)** | 뭉치 여럿 합체 | 검색바 + 메뉴 + 로고 = 헤더 |
| **템플릿 / 페이지** | 동굴 벽 한 면 통째 | 로그인 화면, 대시보드 |

## 3. Props (프롭스) — "넣어주는 재료"
부품한테 주는 명령 쪽지. 버튼한테 *"너 글자는 '사냥', 색은 빨강"* 하고 건넨다.
- 같은 버튼인데 재료만 바꿔 여기저기 쓴다.

## 4. State (스테이트) — "지금 상태"
부품 머릿속 기억. *"불 켜짐? 꺼짐?", "글자 몇 자 적었나?"*
- 상태 바뀌면 화면도 따라 바뀐다.

## 5. Hook (훅) — "마법 갈고리" (React 세계)
부품이 기억하거나(`useState`) 바깥일 할 때(`useEffect`) 거는 갈고리.

## 6. 레이아웃 (Layout) — "어디 놓나"
부품들 자리 배치 규칙.
- **Flexbox** = 한 줄로 쭉 늘어놓기
- **Grid** = 바둑판 칸 나누기

## 7. 스타일링 (Styling) — "어떻게 칠하나"
- **CSS** = 칠하는 기본 물감
- **Tailwind** = 미리 짜둔 물감통 (`p-4`, `text-red-500`)
- **CSS-in-JS / styled-components** = 부품 안에 물감 넣어두기

## 8. 반응형 (Responsive) — "큰 벽, 작은 벽"
큰 동굴 벽(PC)이든 작은 돌판(폰)이든 알아서 모양 맞춤.

## 9. 상태관리 (State Management) — "마을 공동 기억창고"
부품끼리 멀어서 기억 공유 어려울 때, 마을 중앙 창고에 모아둠.
- 예: **Redux, Zustand, Context**

---

## 🔥 큰 그림 (우가 요약)

> 디자인 토큰(약속 쪽지) → 컴포넌트(부품, 작은→큰 단계) → props(재료 넣기) + state(지금 상태)로 살아 움직임 → 레이아웃으로 배치 → 스타일로 칠함 → 반응형으로 어디든 맞춤.

우가우가우가! 🦣🔥
